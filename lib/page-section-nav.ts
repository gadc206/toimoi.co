/** In-page anchors — keep in sync with `id` on each `<section>`. */
export const PAGE_SECTION_NAV = [
  /** Scrolls to the start of the emotional run (Feeling → What we do → Trust). */
  { href: "#feeling", label: "Our approach" },
  { href: "#about", label: "About" },
  { href: "#services", label: "Services" },
  { href: "#referral", label: "Refer a friend" },
  { href: "#closing", label: "When you're ready" },
  { href: "#join", label: "Join our list" },
] as const
